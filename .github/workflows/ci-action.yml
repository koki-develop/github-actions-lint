name: CI Action

on:
  push:
    paths:
      - ".github/workflows/ci-action.yml"
      - "actionlint/**"
      - "ghalint/**"

jobs:
  actionlint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./actionlint

  ghalint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./ghalint
        with:
          action-path: ./**/action.yml
