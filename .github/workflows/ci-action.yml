name: CI Action

on:
  push:
    paths:
      - ".github/workflows/ci-action.yml"
      - "actionlint/**"
      - "ghalint/**"

jobs:
  actionlint:
    timeout-minutes: 5
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: ./actionlint

  ghalint:
    timeout-minutes: 5
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: ./ghalint
        with:
          action-path: ./**/action.yml
